# Python packages needed to run the MedVault backend.
# Install them with:  pip install -r requirements.txt
#
# Kept minimal for Task 1. More packages (SQLAlchemy, Alembic, boto3,
# firebase-admin, etc.) get added in later tasks as we actually use them.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
boto3==1.35.74
alembic==1.14.0
firebase-admin==6.6.0
Pillow==12.3.0
pillow-heif==1.5.0
python-multipart==0.0.32
