# Developer tools - NOT needed to run the app, only to develop it
# (linting, formatting, running tests).
#
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
# (tests import the real app, so requirements.txt is needed too)
#
# httpx used to live only here (for FastAPI's TestClient) - it moved to
# requirements.txt once app code (the Google Vision OCR provider) started
# depending on it directly too.

ruff==0.8.4
black==24.10.0
pytest==8.3.4
